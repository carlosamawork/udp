# F9 Home Page — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir la Home UDP (`front-page.php`) con 11 secciones fijas en orden hardcodeado, cada sección en su propio template-part, con contenido editable vía ACF y datos dinámicos desde CPTs/queries existentes.

**Architecture:** Option B — `front-page.php` como orquestador que llama `get_template_part('template-parts/home/section', $slug)` para cada una de las 11 secciones en orden fijo. Sin reordenación por el usuario. Contenido editable en grupo ACF `group_template_home` (location: `page_type == front_page`). Las secciones de datos usan helpers `udp_query_*` de `inc/udp-cards.php`.

**Tech Stack:** PHP/WordPress, ACF Pro, Bootstrap 5 (grid/utilities), SCSS/BEM, Swiper.js (lazy import), vanilla JS (IntersectionObserver, CSS scroll-driven + fallback)

---

## File Map

### New files
| Archivo | Responsabilidad |
|---------|----------------|
| `front-page.php` | Orquestador: llama 11 secciones en orden fijo |
| `template-parts/home/section-portada.php` | S1: Hero con título, CTA e imagen |
| `template-parts/home/section-buscador-carreras.php` | S2: Formulario GET → `/carreras/` |
| `template-parts/home/section-noticias.php` | S3: Últimas noticias en Swiper |
| `template-parts/home/section-facultades.php` | S4: Marquee de nombres de facultades |
| `template-parts/home/section-eventos.php` | S5: 2 cards grandes + tabla 5 eventos |
| `template-parts/home/section-posttitulos.php` | S6: Destacado azul + foto (ACF) |
| `template-parts/home/section-vida-universitaria.php` | S7: Título + texto + 3 links + imagen/embed |
| `template-parts/home/section-cultura-udp.php` | S8: Repeater categorías con hover fade |
| `template-parts/home/section-cultura-digital.php` | S9: Sección con fondo ACF + contenido |
| `template-parts/home/section-innovacion.php` | S10: Noticias filtradas por categoría |
| `template-parts/home/section-cifras.php` | S11: Repeater de cifras numéricas |
| `src/scss/templates/_home.scss` | Todos los estilos BEM de la home |
| `src/js/modules/home-portada.js` | Animación scroll clip-path (S1) |
| `src/js/modules/home-noticias.js` | Swiper lazy (S3) |
| `src/js/modules/home-cultura-udp.js` | Hover fade imagen (S8) |
| `acf-json/group_template_home.json` | Grupo ACF con todos los campos editables |

### Modified files
| Archivo | Cambio |
|---------|--------|
| `src/scss/main.scss` | Añadir `@import "templates/home";` (línea 95, tras `simple-accordion`) |
| `src/js/main.js` | Importar y llamar los 3 nuevos módulos home |
| `acf-json/group_tax_facultad_meta.json` | Añadir campo `siglas` a la taxonomía facultad |
| `inc/udp-cards.php` | Añadir filtros `fecha_desde` y `order` a `udp_query_agenda()` |

---

## Task 1: Infraestructura — front-page.php + SCSS entry + main.js

**Files:**
- Create: `front-page.php`
- Create: `src/scss/templates/_home.scss`
- Modify: `src/scss/main.scss:95`
- Modify: `src/js/main.js`

- [ ] **Step 1: Crear front-page.php**

```php
<?php
/**
 * Front Page — Home UDP
 *
 * Orquestador de 11 secciones en orden fijo.
 * El contenido de cada sección vive en template-parts/home/section-*.php.
 *
 * @package starter-bs5
 */

get_header();
?>
<main id="main" class="udp-home">
    <?php
    $sections = [
        'portada',
        'buscador-carreras',
        'noticias',
        'facultades',
        'eventos',
        'posttitulos',
        'vida-universitaria',
        'cultura-udp',
        'cultura-digital',
        'innovacion',
        'cifras',
    ];

    foreach ( $sections as $sec ) {
        get_template_part( 'template-parts/home/section', $sec );
    }
    ?>
</main>
<?php get_footer(); ?>
```

- [ ] **Step 2: Crear src/scss/templates/_home.scss (shell vacío con comentario)**

```scss
// =============================================================================
// HOME — Estilos de la front page UDP
// Cada sección tiene su bloque BEM .udp-home-{nombre}.
// =============================================================================
```

- [ ] **Step 3: Añadir import en src/scss/main.scss tras la línea `@import "templates/simple-accordion";`**

Insertar:
```scss
@import "templates/home";
```

- [ ] **Step 4: Añadir imports y calls en src/js/main.js**

Añadir tras `import { initMegaMenu }`:
```js
import { initHomePortada }    from '@modules/home-portada';
import { initHomeNoticias }   from '@modules/home-noticias';
import { initHomeCulturaUdp } from '@modules/home-cultura-udp';
```

Añadir dentro de `domReady(() => { ... })` tras `initMegaMenu()`:
```js
    initHomePortada();
    initHomeNoticias();
    initHomeCulturaUdp();
```

- [ ] **Step 5: Crear los 3 módulos JS como stubs vacíos para que el import no falle**

`src/js/modules/home-portada.js`:
```js
export function initHomePortada() {}
```

`src/js/modules/home-noticias.js`:
```js
export function initHomeNoticias() {}
```

`src/js/modules/home-cultura-udp.js`:
```js
export function initHomeCulturaUdp() {}
```

- [ ] **Step 6: Verificar que el build no tiene errores**

```bash
cd /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme && npm run build 2>&1 | tail -20
```

Esperado: sin errores, `dist/main.css` y `dist/main.js` generados.

- [ ] **Step 7: Commit**

```bash
git add front-page.php src/scss/templates/_home.scss src/scss/main.scss src/js/main.js src/js/modules/home-portada.js src/js/modules/home-noticias.js src/js/modules/home-cultura-udp.js
git commit -m "feat(home): scaffold front-page.php + SCSS entry + JS module stubs"
```

---

## Task 2: ACF JSON — group_template_home + siglas en facultad

**Files:**
- Create: `acf-json/group_template_home.json`
- Modify: `acf-json/group_tax_facultad_meta.json`

- [ ] **Step 1: Crear acf-json/group_template_home.json**

```json
{
    "key": "group_template_home",
    "title": "Home — Contenido por sección",
    "fields": [
        { "key": "field_home_tab_portada", "label": "Portada", "name": "tab_portada", "type": "tab", "placement": "top" },
        { "key": "field_home_portada_titulo", "label": "Título principal", "name": "portada_titulo", "type": "text", "required": 1 },
        { "key": "field_home_portada_subtitulo", "label": "Subtítulo", "name": "portada_subtitulo", "type": "textarea", "rows": 2 },
        { "key": "field_home_portada_cta_texto", "label": "Texto del CTA", "name": "portada_cta_texto", "type": "text" },
        { "key": "field_home_portada_cta_url", "label": "URL del CTA", "name": "portada_cta_url", "type": "url" },
        { "key": "field_home_portada_imagen", "label": "Imagen de portada", "name": "portada_imagen", "type": "image", "return_format": "array", "library": "all" },

        { "key": "field_home_tab_posttitulos", "label": "Postítulos", "name": "tab_posttitulos", "type": "tab", "placement": "top" },
        { "key": "field_home_post_titulo", "label": "Título", "name": "posttitulos_titulo", "type": "text" },
        { "key": "field_home_post_desc", "label": "Descripción", "name": "posttitulos_descripcion", "type": "wysiwyg", "tabs": "all", "media_upload": 0 },
        { "key": "field_home_post_link_texto", "label": "Texto del link", "name": "posttitulos_link_texto", "type": "text" },
        { "key": "field_home_post_link_url", "label": "URL del link", "name": "posttitulos_link_url", "type": "url" },
        { "key": "field_home_post_imagen", "label": "Imagen", "name": "posttitulos_imagen", "type": "image", "return_format": "array", "library": "all" },

        { "key": "field_home_tab_vida", "label": "Vida Universitaria", "name": "tab_vida", "type": "tab", "placement": "top" },
        { "key": "field_home_vida_titulo", "label": "Título", "name": "vida_titulo", "type": "text" },
        { "key": "field_home_vida_texto", "label": "Texto", "name": "vida_texto", "type": "textarea", "rows": 4 },
        {
            "key": "field_home_vida_links",
            "label": "Links (máx. 3)",
            "name": "vida_links",
            "type": "repeater",
            "max": 3,
            "button_label": "Añadir link",
            "sub_fields": [
                { "key": "field_home_vida_link_texto", "label": "Texto", "name": "link_texto", "type": "text" },
                { "key": "field_home_vida_link_url",   "label": "URL",   "name": "link_url",   "type": "url" }
            ]
        },
        { "key": "field_home_vida_imagen", "label": "Imagen", "name": "vida_imagen", "type": "image", "return_format": "array", "library": "all", "instructions": "Si hay imagen no se muestra el embed, y viceversa." },
        { "key": "field_home_vida_embed", "label": "Embed de video", "name": "vida_embed", "type": "oembed", "instructions": "URL de YouTube o Vimeo. Solo si no hay imagen." },

        { "key": "field_home_tab_cultura_udp", "label": "Cultura UDP", "name": "tab_cultura_udp", "type": "tab", "placement": "top" },
        {
            "key": "field_home_cultura_udp_items",
            "label": "Categorías culturales",
            "name": "cultura_udp_items",
            "type": "repeater",
            "button_label": "Añadir categoría",
            "sub_fields": [
                { "key": "field_home_cu_nombre",   "label": "Nombre categoría", "name": "cu_nombre",   "type": "text" },
                { "key": "field_home_cu_contador",  "label": "Contador",         "name": "cu_contador",  "type": "number" },
                { "key": "field_home_cu_imagen",    "label": "Imagen",           "name": "cu_imagen",    "type": "image", "return_format": "array" },
                { "key": "field_home_cu_link",      "label": "URL",              "name": "cu_link",      "type": "url" }
            ]
        },

        { "key": "field_home_tab_cultura_digital", "label": "Cultura Digital", "name": "tab_cultura_digital", "type": "tab", "placement": "top" },
        { "key": "field_home_cd_titulo", "label": "Título", "name": "cd_titulo", "type": "text" },
        { "key": "field_home_cd_texto",  "label": "Texto",  "name": "cd_texto",  "type": "textarea", "rows": 4 },
        { "key": "field_home_cd_fondo",  "label": "Imagen de fondo (fondo)", "name": "fondo", "type": "image", "return_format": "array", "library": "all" },
        { "key": "field_home_cd_link_texto", "label": "Texto link", "name": "cd_link_texto", "type": "text" },
        { "key": "field_home_cd_link_url",   "label": "URL link",   "name": "cd_link_url",   "type": "url" },

        { "key": "field_home_tab_cifras", "label": "Cifras", "name": "tab_cifras", "type": "tab", "placement": "top" },
        {
            "key": "field_home_cifras_items",
            "label": "Cifras",
            "name": "cifras_items",
            "type": "repeater",
            "button_label": "Añadir cifra",
            "sub_fields": [
                { "key": "field_home_cifra_numero",      "label": "Número",             "name": "cifra_numero",      "type": "text", "instructions": "Ej: 15.000" },
                { "key": "field_home_cifra_descripcion", "label": "Descripción",        "name": "cifra_descripcion", "type": "text" },
                { "key": "field_home_cifra_ranking",     "label": "Ranking (opcional)", "name": "cifra_ranking",     "type": "text" }
            ]
        }
    ],
    "location": [[{ "param": "page_type", "operator": "==", "value": "front_page" }]],
    "menu_order": 0,
    "position": "normal",
    "style": "default",
    "label_placement": "top",
    "instruction_placement": "label",
    "active": true,
    "description": "Contenido editable de la Home por sección. Location: front_page."
}
```

- [ ] **Step 2: Añadir campo `siglas` a acf-json/group_tax_facultad_meta.json**

El archivo tiene actualmente 2 campos (`color`, `imagen_taxonomia`). Añadir un tercer campo antes del cierre del array `"fields"`:

```json
{
    "key": "field_tax_facultad_siglas",
    "label": "Siglas",
    "name": "siglas",
    "type": "text",
    "instructions": "Ej: FAD, FIA, FCD. Se usa como eyebrow en tarjetas de Innovación e Investigación.",
    "required": 0,
    "maxlength": 10
}
```

- [ ] **Step 3: Sincronizar ACF a base de datos**

```bash
wp eval-file /tmp/acf-sync-home.php --path=/Applications/MAMP/htdocs/udp/cms
```

Contenido de `/tmp/acf-sync-home.php`:
```php
<?php
global $wpdb;

$groups = [
    '/Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/acf-json/group_template_home.json',
    '/Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/acf-json/group_tax_facultad_meta.json',
];

foreach ( $groups as $path ) {
    $json = json_decode( file_get_contents( $path ), true );
    $key  = $json['key'];

    $existing_id = $wpdb->get_var( $wpdb->prepare(
        "SELECT ID FROM {$wpdb->posts} WHERE post_type='acf-field-group' AND post_name=%s AND post_status='publish' LIMIT 1",
        $key
    ) );

    if ( $existing_id ) {
        acf_update_field_group( array_merge( $json, [ 'ID' => (int) $existing_id ] ) );
        echo "Updated: $key (ID $existing_id)\n";
    } else {
        acf_add_field_group( $json );
        echo "Created: $key\n";
    }
}
```

Esperado:
```
Created: group_template_home
Updated: group_tax_facultad_meta (ID XXXXX)
```

- [ ] **Step 4: Verificar en WP Admin → ACF → Grupos de campos**

Abrir `http://localhost:8888/udp/cms/wp-admin/edit.php?post_type=acf-field-group` y comprobar:
- Existe "Home — Contenido por sección" con location `front_page`
- "Facultad — Metadatos" tiene ahora el campo Siglas

- [ ] **Step 5: Commit**

```bash
git add acf-json/group_template_home.json acf-json/group_tax_facultad_meta.json
git commit -m "feat(home/acf): group_template_home + siglas en facultad"
```

---

## Task 3: Extender udp_query_agenda() con fecha_desde y order

**Files:**
- Modify: `inc/udp-cards.php` (función `udp_query_agenda()` aprox. línea 360)

La función actual solo tiene filtro `year` (LIKE) y ordena siempre DESC. Para la Home necesitamos eventos `fecha >= hoy` ordenados ASC.

- [ ] **Step 1: Añadir extracción de los nuevos filtros**

Tras la línea que extrae `$exclude` (aprox. línea 366), añadir:

```php
$order       = strtoupper( (string) ( $filters['order']       ?? 'DESC' ) );
$fecha_desde = trim( (string)       ( $filters['fecha_desde'] ?? '' ) );
```

- [ ] **Step 2: Cambiar el `'order' => 'DESC'` hardcodeado en $args**

Línea aprox. 375, cambiar:
```php
'order'          => 'DESC',
```
por:
```php
'order'          => in_array( $order, [ 'ASC', 'DESC' ], true ) ? $order : 'DESC',
```

- [ ] **Step 3: Reemplazar el bloque if($year) de meta_query**

Bloque actual (aprox. líneas 386-395):
```php
if ( $year > 0 ) {
    $args['meta_query'] = array(
        array(
            'key'     => 'fecha',
            'value'   => sprintf( '%04d', $year ),
            'compare' => 'LIKE',
        ),
    );
}
```

Reemplazar por:
```php
$meta_q = [];
if ( $year > 0 ) {
    $meta_q[] = [
        'key'     => 'fecha',
        'value'   => sprintf( '%04d', $year ),
        'compare' => 'LIKE',
    ];
}
if ( $fecha_desde !== '' ) {
    $meta_q[] = [
        'key'     => 'fecha',
        'value'   => $fecha_desde,
        'compare' => '>=',
        'type'    => 'CHAR',
    ];
}
if ( ! empty( $meta_q ) ) {
    $args['meta_query'] = count( $meta_q ) > 1
        ? array_merge( [ 'relation' => 'AND' ], $meta_q )
        : $meta_q;
}
```

- [ ] **Step 4: PHP lint**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/inc/udp-cards.php
```

Esperado: `No syntax errors detected`

- [ ] **Step 5: Commit**

```bash
git add inc/udp-cards.php
git commit -m "feat(udp-cards): udp_query_agenda acepta fecha_desde y order"
```

---

## Task 4: Section 1 — Portada

**Files:**
- Create: `template-parts/home/section-portada.php`
- Modify: `src/scss/templates/_home.scss` (añadir bloque)
- Modify: `src/js/modules/home-portada.js` (reemplazar stub)

- [ ] **Step 1: Crear template-parts/home/section-portada.php**

```php
<?php
/**
 * Home — Sección 1: Portada
 *
 * ACF fields: portada_titulo, portada_subtitulo, portada_cta_texto,
 *             portada_cta_url, portada_imagen (array).
 *
 * @package starter-bs5
 */

$titulo    = get_field( 'portada_titulo' );
$subtitulo = get_field( 'portada_subtitulo' );
$cta_texto = get_field( 'portada_cta_texto' );
$cta_url   = get_field( 'portada_cta_url' );
$imagen    = get_field( 'portada_imagen' );

if ( ! $titulo ) {
    return;
}

$img_url = ! empty( $imagen['url'] ) ? $imagen['url'] : '';
$img_alt = ! empty( $imagen['alt'] ) ? $imagen['alt'] : '';
?>
<section class="udp-home-portada js-home-portada" aria-label="Portada">
    <div class="udp-home-portada__inner container">
        <div class="udp-home-portada__content">
            <h1 class="udp-home-portada__titulo"><?php echo esc_html( $titulo ); ?></h1>
            <?php if ( $subtitulo ) : ?>
                <p class="udp-home-portada__subtitulo"><?php echo esc_html( $subtitulo ); ?></p>
            <?php endif; ?>
            <?php if ( $cta_texto && $cta_url ) : ?>
                <a href="<?php echo esc_url( $cta_url ); ?>" class="udp-home-portada__cta btn btn-primary">
                    <?php echo esc_html( $cta_texto ); ?>
                </a>
            <?php endif; ?>
        </div>
        <?php if ( $img_url ) : ?>
            <div class="udp-home-portada__media js-portada-media" aria-hidden="true">
                <img
                    src="<?php echo esc_url( $img_url ); ?>"
                    alt="<?php echo esc_attr( $img_alt ); ?>"
                    class="udp-home-portada__imagen"
                    loading="eager"
                    decoding="async"
                    width="<?php echo esc_attr( $imagen['width'] ?? '' ); ?>"
                    height="<?php echo esc_attr( $imagen['height'] ?? '' ); ?>"
                >
            </div>
        <?php endif; ?>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS de portada a src/scss/templates/_home.scss**

```scss
// -----------------------------------------------------------------------------
// S1 — Portada
// -----------------------------------------------------------------------------
.udp-home-portada {
    position: relative;
    overflow: hidden;
    min-height: 80vh;
    display: flex;
    align-items: center;

    &__inner {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
        align-items: center;

        @include media-breakpoint-down(md) {
            grid-template-columns: 1fr;
        }
    }

    &__titulo {
        font-family: $font-family-display;
        font-size: clamp(2.5rem, 6vw, 5rem);
        line-height: 1.05;
        color: $black;
    }

    &__subtitulo {
        font-size: 1.25rem;
        margin-top: 1rem;
        max-width: 40ch;
    }

    &__cta {
        margin-top: 2rem;
    }

    &__media {
        clip-path: inset(0 100% 0 0);
        animation: portada-reveal 0.8s ease-out forwards;
        animation-play-state: paused;

        &.is-visible {
            animation-play-state: running;
        }

        // CSS Scroll-driven animation (Chrome 115+)
        @supports (animation-timeline: scroll()) {
            animation-timeline: view();
            animation-range: entry 0% entry 40%;
            animation-play-state: running;
        }
    }

    &__imagen {
        width: 100%;
        height: auto;
        display: block;
    }
}

@keyframes portada-reveal {
    from { clip-path: inset(0 100% 0 0); }
    to   { clip-path: inset(0 0% 0 0); }
}
```

- [ ] **Step 3: Implementar home-portada.js (JS fallback para browsers sin scroll-driven)**

```js
/**
 * Home — Portada: clip-path reveal via IntersectionObserver
 * Fallback para browsers sin soporte de animation-timeline: scroll().
 */
import { qs } from '@utils/dom';

export function initHomePortada() {
    // Si el browser soporta scroll-driven animations, el CSS lo maneja solo.
    if ( CSS.supports( 'animation-timeline', 'scroll()' ) ) return;

    const media = qs( '.js-portada-media' );
    if ( ! media ) return;

    const observer = new IntersectionObserver(
        ( entries ) => {
            entries.forEach( ( entry ) => {
                if ( entry.isIntersecting ) {
                    entry.target.classList.add( 'is-visible' );
                    observer.unobserve( entry.target );
                }
            } );
        },
        { threshold: 0.1 }
    );

    observer.observe( media );
}
```

- [ ] **Step 4: PHP lint**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-portada.php
```

Esperado: `No syntax errors detected`

- [ ] **Step 5: Build**

```bash
npm run build 2>&1 | grep -E "error|warning|built" | head -10
```

Esperado: sin errores.

- [ ] **Step 6: Commit**

```bash
git add template-parts/home/section-portada.php src/scss/templates/_home.scss src/js/modules/home-portada.js
git commit -m "feat(home): S1 Portada — template, SCSS, JS clip-path reveal"
```

---

## Task 5: Section 2 — Buscador de Carreras

**Files:**
- Create: `template-parts/home/section-buscador-carreras.php`
- Modify: `src/scss/templates/_home.scss`

La sección es un formulario GET que redirige a `/carreras/?udp_facultad=X&udp_s=Y`. Sin AJAX, sin llamada a `udp_query_carreras`.

- [ ] **Step 1: Crear template-parts/home/section-buscador-carreras.php**

```php
<?php
/**
 * Home — Sección 2: Buscador de Carreras
 *
 * Formulario GET → /carreras/?udp_facultad=&udp_s=
 * Sin AJAX: la búsqueda la procesa el archive page-carreras.php.
 *
 * @package starter-bs5
 */

$facultades = get_terms( [
    'taxonomy'   => 'facultad',
    'hide_empty' => false,
    'orderby'    => 'name',
    'order'      => 'ASC',
] );

if ( is_wp_error( $facultades ) ) {
    $facultades = [];
}

$carreras_page = get_page_by_path( 'carreras' );
$carreras_url  = $carreras_page ? get_permalink( $carreras_page ) : home_url( '/carreras/' );
?>
<section class="udp-home-buscador">
    <div class="container">
        <form
            class="udp-home-buscador__form"
            method="get"
            action="<?php echo esc_url( $carreras_url ); ?>"
            role="search"
            aria-label="Buscador de carreras"
        >
            <div class="udp-home-buscador__fields">
                <select
                    name="udp_facultad"
                    class="udp-home-buscador__select form-select"
                    aria-label="Filtrar por facultad"
                >
                    <option value="">Todas las carreras</option>
                    <?php foreach ( $facultades as $fac ) : ?>
                        <option value="<?php echo esc_attr( $fac->term_id ); ?>">
                            <?php echo esc_html( $fac->name ); ?>
                        </option>
                    <?php endforeach; ?>
                </select>

                <input
                    type="search"
                    name="udp_s"
                    class="udp-home-buscador__input form-control"
                    placeholder="Buscar una carrera"
                    aria-label="Buscar carrera por nombre"
                    value=""
                >

                <button type="submit" class="udp-home-buscador__btn btn btn-primary">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16" aria-hidden="true" focusable="false">
                        <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.115-.099zm-5.242 1.156a5.5 5.5 0 1 1 0-11 5.5 5.5 0 0 1 0 11"/>
                    </svg>
                    <span class="visually-hidden">Buscar</span>
                </button>
            </div>
        </form>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS de buscador a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S2 — Buscador de Carreras
// -----------------------------------------------------------------------------
.udp-home-buscador {
    padding: 3rem 0;
    background-color: $gray-100;

    &__form {
        max-width: 860px;
        margin: 0 auto;
    }

    &__fields {
        display: flex;
        gap: 1rem;
        align-items: center;

        @include media-breakpoint-down(md) {
            flex-direction: column;
        }
    }

    &__select {
        flex: 0 0 260px;

        @include media-breakpoint-down(md) {
            flex: 1 1 auto;
            width: 100%;
        }
    }

    &__input {
        flex: 1 1 auto;
    }

    &__btn {
        flex: 0 0 auto;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
}
```

- [ ] **Step 3: PHP lint**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-buscador-carreras.php
```

Esperado: `No syntax errors detected`

- [ ] **Step 4: Commit**

```bash
git add template-parts/home/section-buscador-carreras.php src/scss/templates/_home.scss
git commit -m "feat(home): S2 Buscador Carreras — form GET + estilos"
```

---

## Task 6: Section 3 — Noticias (Swiper)

**Files:**
- Create: `template-parts/home/section-noticias.php`
- Modify: `src/scss/templates/_home.scss`
- Modify: `src/js/modules/home-noticias.js` (reemplazar stub)

`udp_query_noticias()` devuelve `['cards' => [...], 'total' => N, ...]`. Cada card tiene: `href`, `titulo`, `imagen` (array: url, alt), `eyebrow`, `fecha` (ISO Y-m-d).

- [ ] **Step 1: Crear template-parts/home/section-noticias.php**

```php
<?php
/**
 * Home — Sección 3: Noticias
 *
 * Usa udp_query_noticias(['limit'=>8]).
 * Card shape: href, titulo, imagen['url'], imagen['alt'], eyebrow, fecha (Y-m-d).
 *
 * @package starter-bs5
 */

$result = udp_query_noticias( [ 'limit' => 8 ] );
$cards  = $result['cards'] ?? [];

if ( empty( $cards ) ) {
    return;
}
?>
<section class="udp-home-noticias">
    <div class="container">
        <div class="udp-home-noticias__header">
            <h2 class="udp-home-noticias__titulo">Noticias</h2>
            <a href="<?php echo esc_url( home_url( '/noticias/' ) ); ?>" class="udp-home-noticias__ver-mas">
                Ver todas
            </a>
        </div>
    </div>

    <div class="udp-home-noticias__swiper-wrap">
        <div class="swiper js-home-noticias-swiper">
            <div class="swiper-wrapper">
                <?php foreach ( $cards as $card ) : ?>
                    <article class="swiper-slide udp-home-noticias__slide">
                        <a href="<?php echo esc_url( $card['href'] ); ?>" class="udp-home-noticias__card">
                            <?php if ( ! empty( $card['imagen']['url'] ) ) : ?>
                                <div class="udp-home-noticias__card-img">
                                    <img
                                        src="<?php echo esc_url( $card['imagen']['url'] ); ?>"
                                        alt="<?php echo esc_attr( $card['imagen']['alt'] ?? '' ); ?>"
                                        loading="lazy"
                                        decoding="async"
                                    >
                                </div>
                            <?php endif; ?>

                            <div class="udp-home-noticias__card-body">
                                <?php if ( ! empty( $card['eyebrow'] ) ) : ?>
                                    <span class="eyebrow udp-home-noticias__card-eyebrow">
                                        <?php echo esc_html( $card['eyebrow'] ); ?>
                                    </span>
                                <?php endif; ?>
                                <h3 class="udp-home-noticias__card-titulo"><?php echo esc_html( $card['titulo'] ); ?></h3>
                                <?php if ( ! empty( $card['fecha'] ) ) : ?>
                                    <time
                                        class="udp-home-noticias__card-fecha"
                                        datetime="<?php echo esc_attr( $card['fecha'] ); ?>"
                                    >
                                        <?php echo esc_html( date_i18n( 'j \d\e F \d\e Y', strtotime( $card['fecha'] ) ) ); ?>
                                    </time>
                                <?php endif; ?>
                            </div>
                        </a>
                    </article>
                <?php endforeach; ?>
            </div>

            <button class="swiper-button-prev" aria-label="Anterior noticia"></button>
            <button class="swiper-button-next" aria-label="Siguiente noticia"></button>
        </div>
    </div>
</section>
```

- [ ] **Step 2: Implementar home-noticias.js**

```js
/**
 * Home — Noticias Swiper
 * Lazy import de Swiper solo si el componente está presente.
 */
import { qs } from '@utils/dom';

export async function initHomeNoticias() {
    const el = qs( '.js-home-noticias-swiper' );
    if ( ! el ) return;

    const { default: Swiper }             = await import( 'swiper' );
    const { Navigation, Keyboard, FreeMode } = await import( 'swiper/modules' );
    await import( 'swiper/css' );

    new Swiper( el, {
        modules: [ Navigation, Keyboard, FreeMode ],
        slidesPerView: 'auto',
        spaceBetween: 24,
        freeMode: { enabled: true, momentum: true },
        keyboard: { enabled: true },
        grabCursor: true,
        navigation: {
            nextEl: el.querySelector( '.swiper-button-next' ),
            prevEl: el.querySelector( '.swiper-button-prev' ),
        },
        breakpoints: {
            0:   { spaceBetween: 12, slidesOffsetBefore: 16, slidesOffsetAfter: 16 },
            768: { spaceBetween: 24, slidesOffsetBefore: 40, slidesOffsetAfter: 40 },
        },
    } );
}
```

- [ ] **Step 3: Añadir SCSS de noticias a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S3 — Noticias
// -----------------------------------------------------------------------------
.udp-home-noticias {
    padding: 4rem 0;

    &__header {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    &__titulo {
        font-family: $font-family-display;
        font-size: 2rem;
    }

    &__ver-mas {
        font-size: 0.875rem;
        text-decoration: underline;
    }

    &__swiper-wrap {
        overflow: hidden;
    }

    &__slide {
        width: 280px;

        @include media-breakpoint-up(md) {
            width: 320px;
        }
    }

    &__card {
        display: block;
        text-decoration: none;
        color: inherit;
        height: 100%;

        &:hover &-titulo {
            text-decoration: underline;
        }
    }

    &__card-img {
        aspect-ratio: 4/3;
        overflow: hidden;
        margin-bottom: 1rem;

        img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }
    }

    &__card:hover &__card-img img {
        transform: scale(1.04);
    }

    &__card-eyebrow {
        display: block;
        margin-bottom: 0.5rem;
    }

    &__card-titulo {
        font-size: 1rem;
        font-weight: 600;
        line-height: 1.35;
        margin-bottom: 0.5rem;
    }

    &__card-fecha {
        font-size: 0.8125rem;
        color: $gray-600;
    }
}
```

- [ ] **Step 4: PHP lint + build**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-noticias.php && npm run build 2>&1 | grep -E "error|built" | head -5
```

- [ ] **Step 5: Commit**

```bash
git add template-parts/home/section-noticias.php src/scss/templates/_home.scss src/js/modules/home-noticias.js
git commit -m "feat(home): S3 Noticias — Swiper lazy + estilos"
```

---

## Task 7: Section 4 — Facultades (marquee)

**Files:**
- Create: `template-parts/home/section-facultades.php`
- Modify: `src/scss/templates/_home.scss`

Display: marquee animado con CSS (loop infinito doble-copia) + lista de links accesible.

- [ ] **Step 1: Crear template-parts/home/section-facultades.php**

```php
<?php
/**
 * Home — Sección 4: Facultades
 *
 * Marquee de nombres de facultades (CSS loop) + lista de links.
 * Data: get_terms('facultad').
 *
 * @package starter-bs5
 */

$facultades = get_terms( [
    'taxonomy'   => 'facultad',
    'hide_empty' => false,
    'orderby'    => 'name',
    'order'      => 'ASC',
] );

if ( is_wp_error( $facultades ) || empty( $facultades ) ) {
    return;
}
?>
<section class="udp-home-facultades">
    <div class="udp-home-facultades__marquee" aria-hidden="true">
        <div class="udp-home-facultades__track">
            <?php foreach ( $facultades as $fac ) : ?>
                <span class="udp-home-facultades__marquee-item"><?php echo esc_html( $fac->name ); ?></span>
            <?php endforeach; ?>
            <?php // Segunda copia para loop infinito sin salto visual ?>
            <?php foreach ( $facultades as $fac ) : ?>
                <span class="udp-home-facultades__marquee-item" aria-hidden="true"><?php echo esc_html( $fac->name ); ?></span>
            <?php endforeach; ?>
        </div>
    </div>

    <div class="container">
        <nav class="udp-home-facultades__nav" aria-label="Facultades UDP">
            <ul class="udp-home-facultades__lista list-unstyled">
                <?php foreach ( $facultades as $fac ) : ?>
                    <?php $url = get_term_link( $fac ); ?>
                    <li>
                        <a
                            href="<?php echo esc_url( is_wp_error( $url ) ? home_url( '/' ) : $url ); ?>"
                            class="udp-home-facultades__link"
                        >
                            <?php echo esc_html( $fac->name ); ?>
                        </a>
                    </li>
                <?php endforeach; ?>
            </ul>
        </nav>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S4 — Facultades
// -----------------------------------------------------------------------------
.udp-home-facultades {
    padding: 4rem 0;
    overflow: hidden;

    &__marquee {
        overflow: hidden;
        margin-bottom: 3rem;
        white-space: nowrap;
    }

    &__track {
        display: inline-flex;
        gap: 3rem;
        animation: marquee-scroll 30s linear infinite;

        @media (prefers-reduced-motion: reduce) {
            animation: none;
        }
    }

    &__marquee-item {
        font-family: $font-family-display;
        font-size: clamp(2rem, 5vw, 3.5rem);
        color: $gray-300;
        text-transform: uppercase;
        flex-shrink: 0;
    }

    &__lista {
        column-count: 2;
        column-gap: 2rem;

        @include media-breakpoint-up(md) {
            column-count: 3;
        }
    }

    &__link {
        display: block;
        padding: 0.375rem 0;
        color: $black;
        text-decoration: none;
        font-weight: 500;
        border-bottom: 1px solid $gray-200;
        transition: color 0.2s;

        &:hover {
            color: var(--udp-color-primary, #0033a0);
        }
    }
}

@keyframes marquee-scroll {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
}
```

- [ ] **Step 3: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-facultades.php
git add template-parts/home/section-facultades.php src/scss/templates/_home.scss
git commit -m "feat(home): S4 Facultades — marquee CSS + lista links"
```

---

## Task 8: Section 5 — Próximos Eventos

**Files:**
- Create: `template-parts/home/section-eventos.php`
- Modify: `src/scss/templates/_home.scss`

Card shape de agenda: `href`, `titulo`, `imagen['url']`, `imagen['alt']`, `fecha` (ISO), `fecha_display`, `hora_display`, `lugar`.

- [ ] **Step 1: Crear template-parts/home/section-eventos.php**

```php
<?php
/**
 * Home — Sección 5: Próximos Eventos
 *
 * 2 cards grandes destacadas + tabla de hasta 5 eventos adicionales.
 * Usa udp_query_agenda con fecha_desde=hoy, order=ASC, limit=7.
 * Card shape: href, titulo, imagen['url'], imagen['alt'],
 *             fecha (Y-m-d ISO), fecha_display, hora_display, lugar.
 *
 * @package starter-bs5
 */

$hoy   = current_time( 'Ymd' );
$result = udp_query_agenda( [
    'fecha_desde' => $hoy,
    'order'       => 'ASC',
    'limit'       => 7,
] );

$cards = $result['cards'] ?? [];

if ( empty( $cards ) ) {
    return;
}

$destacados = array_slice( $cards, 0, 2 );
$lista      = array_slice( $cards, 2, 5 );
?>
<section class="udp-home-eventos">
    <div class="container">
        <div class="udp-home-eventos__header">
            <h2 class="udp-home-eventos__titulo">Próximos eventos</h2>
            <a href="<?php echo esc_url( home_url( '/agenda/' ) ); ?>" class="udp-home-eventos__ver-mas">
                Ver agenda completa
            </a>
        </div>

        <?php if ( $destacados ) : ?>
            <div class="udp-home-eventos__destacados row g-4">
                <?php foreach ( $destacados as $card ) : ?>
                    <div class="col-md-6">
                        <a href="<?php echo esc_url( $card['href'] ); ?>" class="udp-home-eventos__card">
                            <?php if ( ! empty( $card['imagen']['url'] ) ) : ?>
                                <div class="udp-home-eventos__card-img">
                                    <img
                                        src="<?php echo esc_url( $card['imagen']['url'] ); ?>"
                                        alt="<?php echo esc_attr( $card['imagen']['alt'] ?? '' ); ?>"
                                        loading="lazy"
                                        decoding="async"
                                    >
                                </div>
                            <?php endif; ?>
                            <div class="udp-home-eventos__card-body">
                                <?php if ( ! empty( $card['fecha_display'] ) ) : ?>
                                    <time class="udp-home-eventos__card-fecha" datetime="<?php echo esc_attr( $card['fecha'] ); ?>">
                                        <?php echo esc_html( $card['fecha_display'] ); ?>
                                    </time>
                                <?php endif; ?>
                                <h3 class="udp-home-eventos__card-titulo"><?php echo esc_html( $card['titulo'] ); ?></h3>
                                <?php if ( ! empty( $card['lugar'] ) ) : ?>
                                    <span class="udp-home-eventos__card-lugar"><?php echo esc_html( $card['lugar'] ); ?></span>
                                <?php endif; ?>
                            </div>
                        </a>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>

        <?php if ( $lista ) : ?>
            <table class="udp-home-eventos__tabla">
                <caption class="visually-hidden">Próximos eventos adicionales</caption>
                <tbody>
                    <?php foreach ( $lista as $card ) : ?>
                        <tr>
                            <td class="udp-home-eventos__tabla-fecha">
                                <time datetime="<?php echo esc_attr( $card['fecha'] ); ?>">
                                    <?php echo esc_html( $card['fecha_display'] ?? '' ); ?>
                                </time>
                            </td>
                            <td class="udp-home-eventos__tabla-titulo">
                                <a href="<?php echo esc_url( $card['href'] ); ?>">
                                    <?php echo esc_html( $card['titulo'] ); ?>
                                </a>
                            </td>
                            <?php if ( ! empty( $card['lugar'] ) ) : ?>
                                <td class="udp-home-eventos__tabla-lugar">
                                    <?php echo esc_html( $card['lugar'] ); ?>
                                </td>
                            <?php endif; ?>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        <?php endif; ?>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S5 — Próximos Eventos
// -----------------------------------------------------------------------------
.udp-home-eventos {
    padding: 4rem 0;
    background-color: $gray-100;

    &__header {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    &__titulo {
        font-family: $font-family-display;
        font-size: 2rem;
    }

    &__ver-mas {
        font-size: 0.875rem;
        text-decoration: underline;
    }

    &__card {
        display: block;
        text-decoration: none;
        color: inherit;
        background: $white;
        height: 100%;

        &:hover &-titulo {
            text-decoration: underline;
        }
    }

    &__card-img {
        aspect-ratio: 16/9;
        overflow: hidden;

        img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }
    }

    &__card:hover &__card-img img {
        transform: scale(1.04);
    }

    &__card-body {
        padding: 1.25rem;
    }

    &__card-fecha {
        display: block;
        font-size: 0.8125rem;
        color: $gray-600;
        margin-bottom: 0.5rem;
    }

    &__card-titulo {
        font-size: 1.125rem;
        font-weight: 600;
        line-height: 1.3;
        margin-bottom: 0.5rem;
    }

    &__card-lugar {
        font-size: 0.8125rem;
        color: $gray-600;
    }

    &__tabla {
        width: 100%;
        margin-top: 2rem;
        border-collapse: collapse;

        tr {
            border-bottom: 1px solid $gray-200;

            &:last-child { border-bottom: none; }
        }

        td {
            padding: 0.875rem 0;
            vertical-align: middle;
        }
    }

    &__tabla-fecha {
        color: $gray-600;
        font-size: 0.875rem;
        white-space: nowrap;
        padding-right: 1.5rem;
    }

    &__tabla-titulo {
        a {
            color: $black;
            text-decoration: none;
            font-weight: 500;

            &:hover { text-decoration: underline; }
        }
    }

    &__tabla-lugar {
        font-size: 0.875rem;
        color: $gray-600;
        padding-left: 1rem;
        text-align: right;
    }
}
```

- [ ] **Step 3: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-eventos.php
git add template-parts/home/section-eventos.php src/scss/templates/_home.scss
git commit -m "feat(home): S5 Próximos Eventos — cards grandes + tabla"
```

---

## Task 9: Section 6 — Postítulos (Destacado azul + foto)

**Files:**
- Create: `template-parts/home/section-posttitulos.php`
- Modify: `src/scss/templates/_home.scss`

Sección de fondo azul, texto a la izquierda, imagen a la derecha. Datos 100% ACF.

- [ ] **Step 1: Crear template-parts/home/section-posttitulos.php**

```php
<?php
/**
 * Home — Sección 6: Postítulos (Destacado azul + foto)
 *
 * ACF fields: posttitulos_titulo, posttitulos_descripcion (wysiwyg),
 *             posttitulos_link_texto, posttitulos_link_url,
 *             posttitulos_imagen (array).
 *
 * @package starter-bs5
 */

$titulo      = get_field( 'posttitulos_titulo' );
$descripcion = get_field( 'posttitulos_descripcion' );
$link_texto  = get_field( 'posttitulos_link_texto' );
$link_url    = get_field( 'posttitulos_link_url' );
$imagen      = get_field( 'posttitulos_imagen' );

if ( ! $titulo && ! $descripcion ) {
    return;
}

$img_url = ! empty( $imagen['url'] ) ? $imagen['url'] : '';
$img_alt = ! empty( $imagen['alt'] ) ? $imagen['alt'] : '';
?>
<section class="udp-home-posttitulos">
    <div class="container">
        <div class="udp-home-posttitulos__inner row align-items-center g-5">
            <div class="col-lg-6 udp-home-posttitulos__content">
                <?php if ( $titulo ) : ?>
                    <h2 class="udp-home-posttitulos__titulo"><?php echo esc_html( $titulo ); ?></h2>
                <?php endif; ?>
                <?php if ( $descripcion ) : ?>
                    <div class="udp-home-posttitulos__descripcion">
                        <?php echo wp_kses_post( $descripcion ); ?>
                    </div>
                <?php endif; ?>
                <?php if ( $link_texto && $link_url ) : ?>
                    <a href="<?php echo esc_url( $link_url ); ?>" class="udp-home-posttitulos__cta btn btn-outline-light mt-4">
                        <?php echo esc_html( $link_texto ); ?>
                    </a>
                <?php endif; ?>
            </div>
            <?php if ( $img_url ) : ?>
                <div class="col-lg-6 udp-home-posttitulos__media">
                    <img
                        src="<?php echo esc_url( $img_url ); ?>"
                        alt="<?php echo esc_attr( $img_alt ); ?>"
                        loading="lazy"
                        decoding="async"
                        class="udp-home-posttitulos__imagen img-fluid"
                        width="<?php echo esc_attr( $imagen['width'] ?? '' ); ?>"
                        height="<?php echo esc_attr( $imagen['height'] ?? '' ); ?>"
                    >
                </div>
            <?php endif; ?>
        </div>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S6 — Postítulos (Destacado azul + foto)
// -----------------------------------------------------------------------------
.udp-home-posttitulos {
    padding: 5rem 0;
    background-color: #0033a0; // Azul UDP
    color: $white;

    &__titulo {
        font-family: $font-family-display;
        font-size: clamp(1.75rem, 4vw, 3rem);
        color: $white;
        line-height: 1.1;
        margin-bottom: 1.5rem;
    }

    &__descripcion {
        font-size: 1rem;
        line-height: 1.7;
        opacity: 0.9;

        p { margin-bottom: 1rem; }
        a { color: $white; }
    }

    &__imagen {
        border-radius: 2px;
    }
}
```

- [ ] **Step 3: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-posttitulos.php
git add template-parts/home/section-posttitulos.php src/scss/templates/_home.scss
git commit -m "feat(home): S6 Postítulos — destacado azul + foto"
```

---

## Task 10: Section 7 — Vida Universitaria

**Files:**
- Create: `template-parts/home/section-vida-universitaria.php`
- Modify: `src/scss/templates/_home.scss`

ACF fields: `vida_titulo`, `vida_texto`, `vida_links` (repeater: link_texto, link_url), `vida_imagen` (array), `vida_embed` (oembed string). Imagen y embed son mutuamente excluyentes (prioridad imagen).

- [ ] **Step 1: Crear template-parts/home/section-vida-universitaria.php**

```php
<?php
/**
 * Home — Sección 7: Vida Universitaria
 *
 * ACF fields: vida_titulo, vida_texto, vida_links (repeater),
 *             vida_imagen (array), vida_embed (oembed string).
 * La imagen tiene prioridad sobre el embed.
 *
 * @package starter-bs5
 */

$titulo  = get_field( 'vida_titulo' );
$texto   = get_field( 'vida_texto' );
$links   = get_field( 'vida_links' );
$imagen  = get_field( 'vida_imagen' );
$embed   = get_field( 'vida_embed' );

if ( ! $titulo && ! $texto ) {
    return;
}

$img_url  = ! empty( $imagen['url'] ) ? $imagen['url'] : '';
$img_alt  = ! empty( $imagen['alt'] ) ? $imagen['alt'] : '';
$has_media = $img_url || $embed;
?>
<section class="udp-home-vida">
    <div class="container">
        <div class="udp-home-vida__inner row g-5 align-items-center">
            <div class="col-lg-<?php echo $has_media ? '5' : '12'; ?> udp-home-vida__content">
                <?php if ( $titulo ) : ?>
                    <h2 class="udp-home-vida__titulo"><?php echo esc_html( $titulo ); ?></h2>
                <?php endif; ?>
                <?php if ( $texto ) : ?>
                    <p class="udp-home-vida__texto"><?php echo esc_html( $texto ); ?></p>
                <?php endif; ?>
                <?php if ( ! empty( $links ) ) : ?>
                    <ul class="udp-home-vida__links list-unstyled">
                        <?php foreach ( $links as $item ) : ?>
                            <?php if ( empty( $item['link_url'] ) ) continue; ?>
                            <li class="udp-home-vida__links-item">
                                <a href="<?php echo esc_url( $item['link_url'] ); ?>" class="udp-home-vida__link">
                                    <?php echo esc_html( $item['link_texto'] ); ?>
                                </a>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php endif; ?>
            </div>

            <?php if ( $has_media ) : ?>
                <div class="col-lg-7 udp-home-vida__media">
                    <?php if ( $img_url ) : ?>
                        <img
                            src="<?php echo esc_url( $img_url ); ?>"
                            alt="<?php echo esc_attr( $img_alt ); ?>"
                            class="udp-home-vida__imagen img-fluid"
                            loading="lazy"
                            decoding="async"
                            width="<?php echo esc_attr( $imagen['width'] ?? '' ); ?>"
                            height="<?php echo esc_attr( $imagen['height'] ?? '' ); ?>"
                        >
                    <?php elseif ( $embed ) : ?>
                        <div class="udp-home-vida__embed ratio ratio-16x9">
                            <?php echo wp_oembed_get( esc_url_raw( $embed ) ); // phpcs:ignore WordPress.Security.EscapeOutput.OutputNotEscaped -- oembed output is safe ?>
                        </div>
                    <?php endif; ?>
                </div>
            <?php endif; ?>
        </div>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S7 — Vida Universitaria
// -----------------------------------------------------------------------------
.udp-home-vida {
    padding: 5rem 0;

    &__titulo {
        font-family: $font-family-display;
        font-size: clamp(1.75rem, 3.5vw, 2.5rem);
        margin-bottom: 1rem;
    }

    &__texto {
        font-size: 1rem;
        line-height: 1.7;
        color: $gray-700;
        margin-bottom: 2rem;
        max-width: 55ch;
    }

    &__links {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    &__link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 600;
        color: $black;
        text-decoration: none;
        border-bottom: 2px solid currentColor;
        padding-bottom: 0.125rem;
        transition: opacity 0.2s;

        &:hover { opacity: 0.7; }
    }

    &__imagen {
        border-radius: 2px;
    }

    &__embed {
        border-radius: 2px;
        overflow: hidden;
    }
}
```

- [ ] **Step 3: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-vida-universitaria.php
git add template-parts/home/section-vida-universitaria.php src/scss/templates/_home.scss
git commit -m "feat(home): S7 Vida Universitaria — ACF + imagen/embed"
```

---

## Task 11: Section 8 — Cultura UDP (hover fade)

**Files:**
- Create: `template-parts/home/section-cultura-udp.php`
- Modify: `src/scss/templates/_home.scss`
- Modify: `src/js/modules/home-cultura-udp.js` (reemplazar stub)

Layout: columna derecha = lista de categorías (fondo `#232323`, Arizona Flare 48px). Al hacer hover en un item, la imagen izquierda cambia con fade CSS. El item activo se muestra en `#FF7064`.

- [ ] **Step 1: Crear template-parts/home/section-cultura-udp.php**

```php
<?php
/**
 * Home — Sección 8: Cultura UDP
 *
 * ACF field: cultura_udp_items (repeater).
 * Sub-fields: cu_nombre, cu_contador (number), cu_imagen (array), cu_link (url).
 *
 * JS: home-cultura-udp.js maneja el hover fade de imágenes.
 *
 * @package starter-bs5
 */

$items = get_field( 'cultura_udp_items' );

if ( empty( $items ) ) {
    return;
}

$first = $items[0];
?>
<section class="udp-home-cultura-udp js-cultura-udp">
    <div class="udp-home-cultura-udp__media" aria-hidden="true">
        <?php foreach ( $items as $i => $item ) : ?>
            <?php
            $img_url = ! empty( $item['cu_imagen']['url'] ) ? $item['cu_imagen']['url'] : '';
            $img_alt = ! empty( $item['cu_imagen']['alt'] ) ? $item['cu_imagen']['alt'] : '';
            ?>
            <img
                src="<?php echo esc_url( $img_url ); ?>"
                alt="<?php echo esc_attr( $img_alt ); ?>"
                class="udp-home-cultura-udp__img js-cultura-img <?php echo $i === 0 ? 'is-active' : ''; ?>"
                data-index="<?php echo esc_attr( $i ); ?>"
                loading="<?php echo $i === 0 ? 'eager' : 'lazy'; ?>"
                decoding="async"
            >
        <?php endforeach; ?>
    </div>

    <div class="udp-home-cultura-udp__panel">
        <ul class="udp-home-cultura-udp__lista list-unstyled" role="list">
            <?php foreach ( $items as $i => $item ) : ?>
                <li
                    class="udp-home-cultura-udp__item js-cultura-item <?php echo $i === 0 ? 'is-active' : ''; ?>"
                    data-index="<?php echo esc_attr( $i ); ?>"
                >
                    <a
                        href="<?php echo esc_url( $item['cu_link'] ?? '#' ); ?>"
                        class="udp-home-cultura-udp__link"
                    >
                        <span class="udp-home-cultura-udp__nombre">
                            <?php echo esc_html( $item['cu_nombre'] ); ?>
                        </span>
                        <?php if ( ! empty( $item['cu_contador'] ) ) : ?>
                            <span class="udp-home-cultura-udp__contador">
                                <?php echo esc_html( $item['cu_contador'] ); ?>
                            </span>
                        <?php endif; ?>
                    </a>
                </li>
            <?php endforeach; ?>
        </ul>
    </div>
</section>
```

- [ ] **Step 2: Implementar home-cultura-udp.js**

```js
/**
 * Home — Cultura UDP: hover fade de imagen.
 * Al hacer hover sobre un item de la lista, activa la imagen correspondiente
 * con una transición de opacidad CSS.
 */
import { qsa } from '@utils/dom';

export function initHomeCulturaUdp() {
    const section = document.querySelector( '.js-cultura-udp' );
    if ( ! section ) return;

    const items  = qsa( '.js-cultura-item', section );
    const images = qsa( '.js-cultura-img',  section );

    if ( ! items.length || ! images.length ) return;

    items.forEach( ( item ) => {
        item.addEventListener( 'mouseenter', () => {
            const idx = item.dataset.index;

            items.forEach(  ( el ) => el.classList.toggle( 'is-active', el.dataset.index === idx ) );
            images.forEach( ( el ) => el.classList.toggle( 'is-active', el.dataset.index === idx ) );
        } );
    } );
}
```

- [ ] **Step 3: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S8 — Cultura UDP
// -----------------------------------------------------------------------------
.udp-home-cultura-udp {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 560px;

    @include media-breakpoint-down(md) {
        grid-template-columns: 1fr;
    }

    &__media {
        position: relative;
        overflow: hidden;
        background: $gray-200;
    }

    &__img {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0;
        transition: opacity 0.4s ease;

        &.is-active {
            opacity: 1;
        }
    }

    &__panel {
        background-color: #232323;
        color: $white;
        display: flex;
        align-items: center;
        padding: 3rem 2.5rem;
    }

    &__lista {
        width: 100%;
    }

    &__item {
        border-bottom: 1px solid rgba(255,255,255,0.1);

        &:last-child { border-bottom: none; }

        &.is-active &__nombre,
        &:hover &__nombre {
            color: #FF7064;
        }
    }

    &__link {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
        padding: 1rem 0;
        text-decoration: none;
        color: inherit;
    }

    &__nombre {
        font-family: $font-family-display;
        font-size: clamp(1.75rem, 3.5vw, 3rem);
        transition: color 0.25s ease;
        line-height: 1.1;
    }

    &__contador {
        font-size: 0.875rem;
        color: rgba(255,255,255,0.5);
        flex-shrink: 0;
    }
}
```

- [ ] **Step 4: PHP lint + build**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-cultura-udp.php && npm run build 2>&1 | grep -E "error|built" | head -5
```

- [ ] **Step 5: Commit**

```bash
git add template-parts/home/section-cultura-udp.php src/scss/templates/_home.scss src/js/modules/home-cultura-udp.js
git commit -m "feat(home): S8 Cultura UDP — hover fade imagen + estilos"
```

---

## Task 12: Section 9 — Cultura Digital

**Files:**
- Create: `template-parts/home/section-cultura-digital.php`
- Modify: `src/scss/templates/_home.scss`

ACF fields: `cd_titulo`, `cd_texto`, `fondo` (imagen de fondo), `cd_link_texto`, `cd_link_url`.

- [ ] **Step 1: Crear template-parts/home/section-cultura-digital.php**

```php
<?php
/**
 * Home — Sección 9: Cultura Digital
 *
 * ACF fields: cd_titulo, cd_texto, fondo (imagen de fondo, array),
 *             cd_link_texto, cd_link_url.
 *
 * @package starter-bs5
 */

$titulo     = get_field( 'cd_titulo' );
$texto      = get_field( 'cd_texto' );
$fondo      = get_field( 'fondo' );
$link_texto = get_field( 'cd_link_texto' );
$link_url   = get_field( 'cd_link_url' );

if ( ! $titulo && ! $texto ) {
    return;
}

$fondo_url = ! empty( $fondo['url'] ) ? $fondo['url'] : '';
$fondo_alt = ! empty( $fondo['alt'] ) ? $fondo['alt'] : '';

$style = $fondo_url
    ? 'style="background-image: url(' . esc_url( $fondo_url ) . ');"'
    : '';
?>
<section class="udp-home-cultura-digital" <?php echo $style; // phpcs:ignore WordPress.Security.EscapeOutput.OutputNotEscaped -- URL ya escapada con esc_url() arriba ?>>
    <div class="udp-home-cultura-digital__overlay"></div>
    <div class="container udp-home-cultura-digital__inner">
        <?php if ( $titulo ) : ?>
            <h2 class="udp-home-cultura-digital__titulo"><?php echo esc_html( $titulo ); ?></h2>
        <?php endif; ?>
        <?php if ( $texto ) : ?>
            <p class="udp-home-cultura-digital__texto"><?php echo esc_html( $texto ); ?></p>
        <?php endif; ?>
        <?php if ( $link_texto && $link_url ) : ?>
            <a href="<?php echo esc_url( $link_url ); ?>" class="udp-home-cultura-digital__cta btn btn-outline-light">
                <?php echo esc_html( $link_texto ); ?>
            </a>
        <?php endif; ?>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S9 — Cultura Digital
// -----------------------------------------------------------------------------
.udp-home-cultura-digital {
    position: relative;
    padding: 6rem 0;
    background-size: cover;
    background-position: center;
    background-color: $gray-900;
    color: $white;

    &__overlay {
        position: absolute;
        inset: 0;
        background: rgba(0,0,0,0.55);
    }

    &__inner {
        position: relative;
        z-index: 1;
        max-width: 680px;
    }

    &__titulo {
        font-family: $font-family-display;
        font-size: clamp(2rem, 5vw, 3.5rem);
        color: $white;
        margin-bottom: 1rem;
    }

    &__texto {
        font-size: 1.125rem;
        line-height: 1.7;
        opacity: 0.9;
        margin-bottom: 2rem;
        max-width: 55ch;
    }
}
```

- [ ] **Step 3: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-cultura-digital.php
git add template-parts/home/section-cultura-digital.php src/scss/templates/_home.scss
git commit -m "feat(home): S9 Cultura Digital — fondo ACF + overlay"
```

---

## Task 13: Section 10 — Innovación e Investigación

**Files:**
- Create: `template-parts/home/section-innovacion.php`
- Modify: `src/scss/templates/_home.scss`

Query: `post_type=post` filtrado por categorías de slug `investigacion` e `innovacion` (OR). Eyebrow = campo `siglas` de la taxonomía `facultad` del post (añadido en Task 2).

- [ ] **Step 1: Obtener IDs de categorías por slug en la sección**

La query usa los term IDs de `investigacion` e `innovacion`. Se resuelven dinámicamente con `get_term_by`. Si alguna no existe, se omite sin error.

- [ ] **Step 2: Crear template-parts/home/section-innovacion.php**

```php
<?php
/**
 * Home — Sección 10: Innovación e Investigación
 *
 * Query: post_type=post, categorías 'investigacion' y/o 'innovacion' (OR).
 * Eyebrow: campo ACF 'siglas' del primer término de taxonomía 'facultad' del post.
 * Requiere que las categorías existan con esos slugs.
 *
 * @package starter-bs5
 */

// Resolver IDs dinámicamente para no hardcodear.
$slugs  = [ 'investigacion', 'innovacion' ];
$cat_ids = [];
foreach ( $slugs as $slug ) {
    $term = get_term_by( 'slug', $slug, 'category' );
    if ( $term && ! is_wp_error( $term ) ) {
        $cat_ids[] = $term->term_id;
    }
}

if ( empty( $cat_ids ) ) {
    // Las categorías no existen aún — silencio para no romper el front.
    return;
}

$query = new WP_Query( [
    'post_type'      => 'post',
    'post_status'    => 'publish',
    'posts_per_page' => 4,
    'orderby'        => 'date',
    'order'          => 'DESC',
    'no_found_rows'  => true,
    'tax_query'      => [
        [
            'taxonomy' => 'category',
            'field'    => 'term_id',
            'terms'    => $cat_ids,
            'operator' => 'IN',
        ],
    ],
] );

if ( ! $query->have_posts() ) {
    return;
}

$posts = $query->posts;
wp_reset_postdata();
?>
<section class="udp-home-innovacion">
    <div class="container">
        <div class="udp-home-innovacion__header">
            <h2 class="udp-home-innovacion__titulo">Innovación e Investigación</h2>
            <a href="<?php echo esc_url( home_url( '/noticias/' ) ); ?>" class="udp-home-innovacion__ver-mas">
                Ver todas
            </a>
        </div>

        <div class="udp-home-innovacion__grid row g-4">
            <?php foreach ( $posts as $post ) : ?>
                <?php
                // Eyebrow: siglas de la primera facultad asociada al post.
                $facs  = get_the_terms( $post->ID, 'facultad' );
                $siglas = '';
                if ( ! is_wp_error( $facs ) && ! empty( $facs ) ) {
                    $siglas = get_field( 'siglas', 'facultad_' . $facs[0]->term_id );
                }

                $thumb_url = get_the_post_thumbnail_url( $post->ID, 'medium_large' );
                ?>
                <article class="col-md-6 col-lg-3">
                    <a href="<?php echo esc_url( get_permalink( $post ) ); ?>" class="udp-home-innovacion__card">
                        <?php if ( $thumb_url ) : ?>
                            <div class="udp-home-innovacion__card-img">
                                <img
                                    src="<?php echo esc_url( $thumb_url ); ?>"
                                    alt=""
                                    loading="lazy"
                                    decoding="async"
                                >
                            </div>
                        <?php endif; ?>
                        <div class="udp-home-innovacion__card-body">
                            <?php if ( $siglas ) : ?>
                                <span class="eyebrow udp-home-innovacion__eyebrow"><?php echo esc_html( $siglas ); ?></span>
                            <?php endif; ?>
                            <h3 class="udp-home-innovacion__card-titulo"><?php echo esc_html( get_the_title( $post ) ); ?></h3>
                            <time
                                class="udp-home-innovacion__fecha"
                                datetime="<?php echo esc_attr( get_the_date( 'Y-m-d', $post ) ); ?>"
                            >
                                <?php echo esc_html( get_the_date( 'j \d\e F \d\e Y', $post ) ); ?>
                            </time>
                        </div>
                    </a>
                </article>
            <?php endforeach; ?>
        </div>
    </div>
</section>
```

- [ ] **Step 3: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S10 — Innovación e Investigación
// -----------------------------------------------------------------------------
.udp-home-innovacion {
    padding: 4rem 0;
    background-color: $gray-100;

    &__header {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    &__titulo {
        font-family: $font-family-display;
        font-size: 2rem;
    }

    &__ver-mas {
        font-size: 0.875rem;
        text-decoration: underline;
    }

    &__card {
        display: block;
        text-decoration: none;
        color: inherit;
        height: 100%;

        &:hover &-titulo {
            text-decoration: underline;
        }
    }

    &__card-img {
        aspect-ratio: 4/3;
        overflow: hidden;
        margin-bottom: 1rem;

        img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    }

    &__eyebrow {
        display: block;
        margin-bottom: 0.5rem;
    }

    &__card-titulo {
        font-size: 1rem;
        font-weight: 600;
        line-height: 1.35;
        margin-bottom: 0.5rem;
    }

    &__fecha {
        font-size: 0.8125rem;
        color: $gray-600;
    }
}
```

- [ ] **Step 4: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-innovacion.php
git add template-parts/home/section-innovacion.php src/scss/templates/_home.scss
git commit -m "feat(home): S10 Innovación — query posts por categoría + eyebrow siglas"
```

---

## Task 14: Section 11 — Cifras

**Files:**
- Create: `template-parts/home/section-cifras.php`
- Modify: `src/scss/templates/_home.scss`

ACF field: `cifras_items` (repeater: `cifra_numero`, `cifra_descripcion`, `cifra_ranking`).

- [ ] **Step 1: Crear template-parts/home/section-cifras.php**

```php
<?php
/**
 * Home — Sección 11: Cifras
 *
 * ACF field: cifras_items (repeater).
 * Sub-fields: cifra_numero (text), cifra_descripcion (text),
 *             cifra_ranking (text, opcional).
 *
 * @package starter-bs5
 */

$cifras = get_field( 'cifras_items' );

if ( empty( $cifras ) ) {
    return;
}
?>
<section class="udp-home-cifras">
    <div class="container">
        <ul class="udp-home-cifras__grid list-unstyled row g-4" role="list">
            <?php foreach ( $cifras as $cifra ) : ?>
                <li class="col-6 col-md-4 col-lg-3 udp-home-cifras__item">
                    <span class="udp-home-cifras__numero" aria-hidden="true">
                        <?php echo esc_html( $cifra['cifra_numero'] ); ?>
                    </span>
                    <span class="udp-home-cifras__descripcion">
                        <?php echo esc_html( $cifra['cifra_descripcion'] ); ?>
                    </span>
                    <?php if ( ! empty( $cifra['cifra_ranking'] ) ) : ?>
                        <span class="udp-home-cifras__ranking">
                            <?php echo esc_html( $cifra['cifra_ranking'] ); ?>
                        </span>
                    <?php endif; ?>
                </li>
            <?php endforeach; ?>
        </ul>
    </div>
</section>
```

- [ ] **Step 2: Añadir SCSS a _home.scss**

```scss
// -----------------------------------------------------------------------------
// S11 — Cifras
// -----------------------------------------------------------------------------
.udp-home-cifras {
    padding: 5rem 0;
    background-color: #0033a0;
    color: $white;

    &__grid {
        justify-content: center;
    }

    &__item {
        text-align: center;
    }

    &__numero {
        display: block;
        font-family: $font-family-display;
        font-size: clamp(2.5rem, 6vw, 4.5rem);
        line-height: 1;
        margin-bottom: 0.5rem;
    }

    &__descripcion {
        display: block;
        font-size: 0.9375rem;
        line-height: 1.4;
        opacity: 0.85;
    }

    &__ranking {
        display: block;
        font-size: 0.75rem;
        opacity: 0.6;
        margin-top: 0.375rem;
        font-style: italic;
    }
}
```

- [ ] **Step 3: PHP lint + commit**

```bash
/Applications/MAMP/bin/php/php8.4.17/bin/php -l template-parts/home/section-cifras.php
git add template-parts/home/section-cifras.php src/scss/templates/_home.scss
git commit -m "feat(home): S11 Cifras — repeater ACF + estilos"
```

---

## Task 15: Build final + smoke test

**Files:**
- Todos los archivos del plan ya están en su estado final

- [ ] **Step 1: Build de producción sin errores**

```bash
cd /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme && npm run build 2>&1 | tail -20
```

Esperado: `✓ built in Xs` sin errores. `dist/main.css` y `dist/main.js` generados.

- [ ] **Step 2: PHP lint de todos los archivos nuevos**

```bash
find template-parts/home -name "*.php" -exec /Applications/MAMP/bin/php/php8.4.17/bin/php -l {} \;
/Applications/MAMP/bin/php/php8.4.17/bin/php -l front-page.php
```

Esperado: `No syntax errors detected` en cada archivo.

- [ ] **Step 3: Verificar que WordPress sirve la home con el template correcto**

Visitar `http://localhost:8888/udp/cms/?theme=new` (con el mu-plugin theme switcher activo).

Comprobar en el HTML:
- `<main class="udp-home">` presente
- Las 11 secciones con sus clases BEM existen en el DOM

```bash
curl -s "http://localhost:8888/udp/cms/?theme=new" | grep -c "udp-home-"
```

Esperado: ≥ 11 matches.

- [ ] **Step 4: Verificar que el ACF aparece en el admin**

Visitar `http://localhost:8888/udp/cms/wp-admin/` → Páginas → Inicio (o la página configurada como front_page) → verificar que aparece el meta-box "Home — Contenido por sección" con las 6 tabs.

- [ ] **Step 5: Commit final**

```bash
git add -A
git commit -m "feat(home): F9 Home — 11 secciones completas, ACF, SCSS, JS"
```

---

## Self-review

### Spec coverage
| Sección | Task | ✓ |
|---------|------|---|
| S1 Portada (ACF + clip-path) | Task 4 | ✓ |
| S2 Buscador Carreras (form GET) | Task 5 | ✓ |
| S3 Noticias (Swiper) | Task 6 | ✓ |
| S4 Facultades (marquee) | Task 7 | ✓ |
| S5 Eventos (cards + tabla) | Task 8 | ✓ |
| S6 Postítulos (ACF) | Task 9 | ✓ |
| S7 Vida Universitaria (ACF + embed) | Task 10 | ✓ |
| S8 Cultura UDP (hover fade) | Task 11 | ✓ |
| S9 Cultura Digital (ACF fondo) | Task 12 | ✓ |
| S10 Innovación (query + siglas) | Task 13 | ✓ |
| S11 Cifras (ACF repeater) | Task 14 | ✓ |
| Extender udp_query_agenda fecha_desde | Task 3 | ✓ |
| Añadir siglas a facultad | Task 2 | ✓ |
| Arquitectura Opción B fija | Task 1 | ✓ |

### Notas de implementación
- `udp_query_noticias()` devuelve `['cards' => [...]]` — acceder siempre con `$result['cards'] ?? []`
- `udp_card_data_from_agenda()` usa `imagen` como array (`['url','alt','sizes']`), no string — acceder con `$card['imagen']['url']`
- El campo ACF `fondo` en S9 tiene name exacto `fondo` (sin prefijo `cd_`) para coincidir con lo confirmado por el jefe
- S10 silencia si las categorías `investigacion`/`innovacion` no existen — no rompe el render
- `get_field('siglas', 'facultad_' . $term_id)` es la forma correcta de leer campos ACF de taxonomías
