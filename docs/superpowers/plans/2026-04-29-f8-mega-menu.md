# F8 — Mega-menú completo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Construir el panel mega-menú real que se abre desde el trigger "Menú" del top-bar (stub de F2). Light theme full-viewport con 3 columnas (items principales / submenu del activo / links externos) + top bar interno (Cerrar + logo) + footer (quick links + social). Mobile: single-column con items expandibles. JS vanilla para apertura/cierre + ESC + click outside + lock body scroll.

**Architecture:** Partial nuevo `template-parts/header/mega-menu.php` insertado en header.php. JS module `mega-menu.js` con state machine simple (closed/open/active-item). ACF schema ya existe (group_options_header con menu_principal repeater); se extiende solo con `mega_menu_quick_links` para los links del footer.

**Reference:** Figma `4QlgGMlzNR9Ye344bAFuye` nodeId `3706:22149` (frame con 20+ variantes — variante "Predeterminada" 3706:22154 + "Variante2" 3706:22176 = Pregrado activo).

---

## Inventario

**Crear:**
- `template-parts/header/mega-menu.php`
- `src/js/modules/mega-menu.js`
- `src/scss/layouts/_mega-menu.scss`

**Modificar:**
- `acf-json/group_options_header.json` — añadir `mega_menu_quick_links` repeater al final.
- `header.php` — incluir el partial mega-menu después del top-bar.
- `src/js/main.js` — import + init.
- `src/scss/main.scss` — `@import "layouts/mega-menu";`.

---

## Task 1: ACF — `mega_menu_quick_links`

**File:** `acf-json/group_options_header.json`

- [x] **Step 1: Read structure**

```bash
jq '.fields[] | {name, type, label}' /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/acf-json/group_options_header.json | head -20
```

- [x] **Step 2: Add `mega_menu_quick_links` field via jq**

```bash
JSON_PATH="/Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/acf-json/group_options_header.json"

cat > /tmp/field-quick-links.json <<'EOF'
{
    "key": "field_options_header_mega_quick_links",
    "label": "Mega-menú: Quick Links (footer)",
    "name": "mega_menu_quick_links",
    "type": "repeater",
    "min": 0,
    "layout": "table",
    "button_label": "Agregar quick link",
    "instructions": "Links rápidos en el footer del mega-menú: Bibliotecas, Estudiantes, Alumni, Servicios, UDP University, etc.",
    "sub_fields": [
        { "key": "field_options_header_mega_quick_titulo", "label": "Título", "name": "titulo", "type": "text", "required": 1 },
        { "key": "field_options_header_mega_quick_link",   "label": "URL", "name": "link", "type": "url", "required": 1 },
        { "key": "field_options_header_mega_quick_new_tab", "label": "Abrir en pestaña nueva", "name": "new_tab", "type": "true_false", "ui": 1, "default_value": 0 }
    ]
}
EOF

# Add the field at the end of fields array
jq --slurpfile new /tmp/field-quick-links.json '.fields += [$new[0]]' "$JSON_PATH" > "$JSON_PATH.tmp" && mv "$JSON_PATH.tmp" "$JSON_PATH"

jq empty "$JSON_PATH" && echo "JSON válido"
jq '[.fields[] | .name]' "$JSON_PATH"
```

Expected: `mega_menu_quick_links` aparece como último field.

- [x] **Step 3: Sync DB-direct UPSERT**

`/tmp/acf-sync-header-f8.php`:

```php
<?php
$json = json_decode( file_get_contents( '/Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/acf-json/group_options_header.json' ), true );
global $wpdb;
$existing_id = (int) $wpdb->get_var( $wpdb->prepare(
    "SELECT ID FROM {$wpdb->posts} WHERE post_type='acf-field-group' AND post_name=%s AND post_status='publish' LIMIT 1",
    $json['key']
) );
if ( $existing_id > 0 ) {
    $json['ID'] = $existing_id;
    WP_CLI::log( 'UPDATE id=' . $existing_id );
} else {
    WP_CLI::log( 'CREATE' );
}
$result = acf_import_field_group( $json );
WP_CLI::success( 'id=' . $result['ID'] );
```

```bash
/Applications/MAMP/bin/php/php8.4.1/bin/php /tmp/wp-cli.phar eval-file /tmp/acf-sync-header-f8.php --path=/Applications/MAMP/htdocs/udp/cms 2>&1 | tail -3
```

---

## Task 2: Partial `mega-menu.php` + integración en header.php

- [x] **Step 1: Crear `template-parts/header/mega-menu.php`**

```php
<?php
/**
 * Header > Mega-menú panel
 *
 * Panel light fixed full-viewport con 3 columnas:
 *  - Col 1: items principales (menu_principal repeater)
 *  - Col 2: submenu del item activo
 *  - Col 3: links_externos del item activo
 * Top bar interno: × Cerrar + logo. Footer: quick links + social.
 *
 * El primer item recibe `--active` por defecto (col 2 + 3 lo muestran).
 *
 * @package Starter_Theme
 */

defined( 'ABSPATH' ) || exit;

$menu_items   = function_exists( 'starter_get_option' ) ? starter_get_option( 'menu_principal' ) : array();
$quick_links  = function_exists( 'starter_get_option' ) ? starter_get_option( 'mega_menu_quick_links' ) : array();
$socials      = function_exists( 'udp_get_social_urls' ) ? udp_get_social_urls() : array();

if ( ! is_array( $menu_items ) ) $menu_items  = array();
if ( ! is_array( $quick_links ) ) $quick_links = array();
?>
<div
    id="udp-megamenu-panel"
    class="udp-megamenu"
    role="dialog"
    aria-modal="true"
    aria-labelledby="udp-megamenu-title"
    hidden
>
    <div class="udp-megamenu__top">
        <button type="button" class="udp-megamenu__close" data-udp-megamenu-close aria-label="<?php esc_attr_e( 'Cerrar menú', 'starter-theme' ); ?>">
            <span class="udp-megamenu__close-circle" aria-hidden="true">
                <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
                    <path d="M3 3l8 8M11 3l-8 8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                </svg>
            </span>
            <span class="udp-megamenu__close-label"><?php esc_html_e( 'Cerrar', 'starter-theme' ); ?></span>
        </button>

        <a href="<?php echo esc_url( home_url( '/' ) ); ?>" class="udp-megamenu__logo" aria-label="<?php bloginfo( 'name' ); ?>">
            <?php
            $logo = function_exists( 'udp_get_logo_url' ) ? udp_get_logo_url( 'negro' ) : '';
            if ( ! empty( $logo ) ) :
                ?>
                <img src="<?php echo esc_url( $logo ); ?>" alt="<?php bloginfo( 'name' ); ?>" />
            <?php else : ?>
                <span class="udp-megamenu__logo-text"><?php bloginfo( 'name' ); ?></span>
            <?php endif; ?>
        </a>
    </div>

    <h2 id="udp-megamenu-title" class="visually-hidden"><?php esc_html_e( 'Menú principal', 'starter-theme' ); ?></h2>

    <?php if ( empty( $menu_items ) ) : ?>
        <p class="udp-megamenu__empty"><?php esc_html_e( 'No hay items configurados en el menú principal.', 'starter-theme' ); ?></p>
    <?php else : ?>
        <div class="udp-megamenu__body">

            <ul class="udp-megamenu__primary" role="menu">
                <?php foreach ( $menu_items as $idx => $item ) :
                    $titulo  = $item['titulo_main_link'] ?? '';
                    $main_link = $item['main_link'] ?? '';
                    $new_tab = ! empty( $item['new_tab'] );
                    if ( ! $titulo ) continue;
                    $is_active = $idx === 0;  // first item active by default
                ?>
                    <li class="udp-megamenu__primary-item<?php echo $is_active ? ' udp-megamenu__primary-item--active' : ''; ?>" role="none">
                        <button
                            type="button"
                            class="udp-megamenu__primary-btn"
                            data-udp-megamenu-item="<?php echo esc_attr( $idx ); ?>"
                            aria-expanded="<?php echo $is_active ? 'true' : 'false'; ?>"
                            aria-controls="udp-megamenu-panel-<?php echo (int) $idx; ?>"
                        >
                            <?php echo esc_html( $titulo ); ?>
                        </button>
                    </li>
                <?php endforeach; ?>
            </ul>

            <?php foreach ( $menu_items as $idx => $item ) :
                $titulo         = $item['titulo_main_link'] ?? '';
                $main_link      = $item['main_link'] ?? '';
                $new_tab_main   = ! empty( $item['new_tab'] );
                $submenu        = is_array( $item['submenu'] ?? null ) ? $item['submenu'] : array();
                $links_externos = is_array( $item['links_externos'] ?? null ) ? $item['links_externos'] : array();
                if ( ! $titulo ) continue;
                $is_active = $idx === 0;
            ?>
                <div
                    id="udp-megamenu-panel-<?php echo (int) $idx; ?>"
                    class="udp-megamenu__detail<?php echo $is_active ? ' udp-megamenu__detail--active' : ''; ?>"
                    data-udp-megamenu-detail="<?php echo esc_attr( $idx ); ?>"
                    <?php echo $is_active ? '' : 'hidden'; ?>
                >
                    <ul class="udp-megamenu__submenu">
                        <?php if ( $main_link ) : ?>
                            <li class="udp-megamenu__submenu-item udp-megamenu__submenu-item--main">
                                <a class="udp-megamenu__submenu-link" href="<?php echo esc_url( $main_link ); ?>"
                                    <?php if ( $new_tab_main ) : ?>target="_blank" rel="noopener noreferrer"<?php endif; ?>>
                                    <?php printf( esc_html__( 'Conoce %s', 'starter-theme' ), esc_html( $titulo ) ); ?>
                                    <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
                                        <path d="M3 3h6v6M9 3 3 9" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                                    </svg>
                                </a>
                            </li>
                        <?php endif; ?>
                        <?php foreach ( $submenu as $sub ) :
                            $sub_titulo = $sub['titulo'] ?? '';
                            $sub_link   = $sub['link']   ?? '';
                            $sub_new    = ! empty( $sub['new_tab_check'] );
                            if ( ! $sub_titulo || ! $sub_link ) continue;
                        ?>
                            <li class="udp-megamenu__submenu-item">
                                <a class="udp-megamenu__submenu-link" href="<?php echo esc_url( $sub_link ); ?>"
                                    <?php if ( $sub_new ) : ?>target="_blank" rel="noopener noreferrer"<?php endif; ?>>
                                    <?php echo esc_html( $sub_titulo ); ?>
                                    <?php if ( $sub_new ) : ?>
                                        <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
                                            <path d="M3 3h6v6M9 3 3 9" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                    <?php else : ?>
                                        <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
                                            <path d="M4 3l3 3-3 3" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                    <?php endif; ?>
                                </a>
                            </li>
                        <?php endforeach; ?>
                    </ul>

                    <ul class="udp-megamenu__externos">
                        <?php foreach ( $links_externos as $ext ) :
                            $ext_titulo = $ext['titulo'] ?? '';
                            $ext_link   = $ext['link']   ?? '';
                            if ( ! $ext_titulo || ! $ext_link ) continue;
                        ?>
                            <li class="udp-megamenu__externos-item">
                                <a class="udp-megamenu__externos-link" href="<?php echo esc_url( $ext_link ); ?>" target="_blank" rel="noopener noreferrer">
                                    <?php echo esc_html( $ext_titulo ); ?>
                                    <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
                                        <path d="M3 3h6v6M9 3 3 9" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                                    </svg>
                                </a>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                </div>
            <?php endforeach; ?>

        </div>
    <?php endif; ?>

    <footer class="udp-megamenu__footer">
        <ul class="udp-megamenu__quick-links">
            <?php foreach ( $quick_links as $ql ) :
                $ql_titulo = $ql['titulo'] ?? '';
                $ql_link   = $ql['link']   ?? '';
                $ql_new    = ! empty( $ql['new_tab'] );
                if ( ! $ql_titulo || ! $ql_link ) continue;
            ?>
                <li class="udp-megamenu__quick-item">
                    <a class="udp-megamenu__quick-link" href="<?php echo esc_url( $ql_link ); ?>"
                        <?php if ( $ql_new ) : ?>target="_blank" rel="noopener noreferrer"<?php endif; ?>>
                        <?php echo esc_html( $ql_titulo ); ?>
                        <?php if ( $ql_new ) : ?>
                            <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
                                <path d="M3 3h6v6M9 3 3 9" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                            </svg>
                        <?php endif; ?>
                    </a>
                </li>
            <?php endforeach; ?>
        </ul>

        <?php if ( ! empty( $socials ) ) : ?>
            <ul class="udp-megamenu__socials">
                <?php foreach ( array( 'linkedin', 'instagram', 'youtube' ) as $key ) :
                    $url = $socials[ $key ] ?? '';
                    if ( ! $url ) continue;
                ?>
                    <li class="udp-megamenu__social-item">
                        <a class="udp-megamenu__social-link" href="<?php echo esc_url( $url ); ?>" target="_blank" rel="noopener noreferrer" aria-label="<?php echo esc_attr( ucfirst( $key ) ); ?>">
                            <?php
                            switch ( $key ) {
                                case 'linkedin':
                                    echo '<svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor"><path d="M3 5.5h2.5v8H3v-8zM4.25 1.8a1.45 1.45 0 1 1 0 2.9 1.45 1.45 0 0 1 0-2.9zM7 5.5h2.4v1.1h0a2.6 2.6 0 0 1 2.4-1.3c2.5 0 3 1.6 3 3.7V13.5h-2.5V9.4c0-1 0-2.3-1.4-2.3s-1.6 1.1-1.6 2.2V13.5H7V5.5z"/></svg>';
                                    break;
                                case 'instagram':
                                    echo '<svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="12" height="12" rx="3"/><circle cx="8" cy="8" r="2.5"/><circle cx="11.5" cy="4.5" r="0.5" fill="currentColor"/></svg>';
                                    break;
                                case 'youtube':
                                    echo '<svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor"><path d="M14.7 4.5c-.2-.6-.7-1-1.3-1.2C12.2 3 8 3 8 3s-4.2 0-5.4.3c-.6.2-1.1.6-1.3 1.2C1 5.7 1 8 1 8s0 2.3.3 3.5c.2.6.7 1 1.3 1.2C3.8 13 8 13 8 13s4.2 0 5.4-.3c.6-.2 1.1-.6 1.3-1.2.3-1.2.3-3.5.3-3.5s0-2.3-.3-3.5zM6.5 10.2V5.8L10.4 8l-3.9 2.2z"/></svg>';
                                    break;
                            }
                            ?>
                        </a>
                    </li>
                <?php endforeach; ?>
            </ul>
        <?php endif; ?>
    </footer>
</div>
```

- [x] **Step 2: Modificar `header.php` para incluir el partial**

Edit `header.php`. Después de:

```php
<header class="udp-site-header" role="banner">
	<?php get_template_part( 'template-parts/header/top-bar' ); ?>
</header>
```

Añadir:

```php
<?php get_template_part( 'template-parts/header/mega-menu' ); ?>
```

- [x] **Step 3: Validar PHP**

```bash
PHP=/Applications/MAMP/bin/php/php8.4.1/bin/php
$PHP -l /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/template-parts/header/mega-menu.php
$PHP -l /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme/header.php
```

---

## Task 3: JS module

- [x] **Step 1: Crear `src/js/modules/mega-menu.js`**

```javascript
/**
 * Mega-menu — open/close + active item state machine.
 *
 * Eventos:
 *  - Click en [data-udp-megamenu-toggle] → open
 *  - Click en [data-udp-megamenu-close] → close
 *  - ESC key → close
 *  - Click en [data-udp-megamenu-item="N"] → activa el detail panel N
 *  - Click backdrop fuera del panel body → close (opcional, si UX lo pide)
 *
 * Lock body scroll cuando está abierto. Trap focus básico (Tab dentro del panel).
 */
import { qs, qsa } from '@utils/dom';

const STATE = {
    isOpen: false,
    activeIdx: 0,
    lastFocused: null,
};

function setOpen(panel, open) {
    if (!panel) return;
    STATE.isOpen = open;
    panel.hidden = !open;
    document.documentElement.classList.toggle('udp-megamenu-open', open);
    document.body.classList.toggle('udp-megamenu-open', open);

    const toggle = qs('[data-udp-megamenu-toggle]');
    if (toggle) toggle.setAttribute('aria-expanded', open ? 'true' : 'false');

    if (open) {
        STATE.lastFocused = document.activeElement;
        const closeBtn = panel.querySelector('[data-udp-megamenu-close]');
        if (closeBtn) closeBtn.focus();
    } else {
        if (STATE.lastFocused && typeof STATE.lastFocused.focus === 'function') {
            STATE.lastFocused.focus();
        }
    }
}

function setActiveItem(panel, idx) {
    if (!panel) return;
    STATE.activeIdx = idx;

    qsa('.udp-megamenu__primary-item', panel).forEach((el, i) => {
        el.classList.toggle('udp-megamenu__primary-item--active', i === idx);
    });
    qsa('.udp-megamenu__primary-btn', panel).forEach((btn, i) => {
        btn.setAttribute('aria-expanded', i === idx ? 'true' : 'false');
    });
    qsa('[data-udp-megamenu-detail]', panel).forEach((el) => {
        const detailIdx = parseInt(el.getAttribute('data-udp-megamenu-detail'), 10);
        const isActive = detailIdx === idx;
        el.classList.toggle('udp-megamenu__detail--active', isActive);
        el.hidden = !isActive;
    });
}

export function initMegaMenu() {
    const panel = qs('#udp-megamenu-panel');
    const toggle = qs('[data-udp-megamenu-toggle]');
    if (!panel || !toggle) return;

    toggle.addEventListener('click', () => setOpen(panel, true));

    const closeBtn = panel.querySelector('[data-udp-megamenu-close]');
    if (closeBtn) closeBtn.addEventListener('click', () => setOpen(panel, false));

    qsa('[data-udp-megamenu-item]', panel).forEach((btn) => {
        const idx = parseInt(btn.getAttribute('data-udp-megamenu-item'), 10);
        btn.addEventListener('click', () => setActiveItem(panel, idx));
        btn.addEventListener('mouseenter', () => setActiveItem(panel, idx));
        btn.addEventListener('focus', () => setActiveItem(panel, idx));
    });

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && STATE.isOpen) {
            setOpen(panel, false);
        }
    });
}
```

- [x] **Step 2: Wire en `src/js/main.js`**

Añadir import:

```javascript
import { initMegaMenu } from '@modules/mega-menu';
```

En `domReady(() => { ... })` añadir:

```javascript
initMegaMenu();
```

---

## Task 4: SCSS

- [x] **Step 1: Crear `src/scss/layouts/_mega-menu.scss`**

```scss
// ==========================================================================
// MEGA-MENU — panel light fixed full-viewport con 3 columnas + footer.
// Top bar interno: × Cerrar + logo. Mobile: single-col.
// ==========================================================================

// Body lock cuando el menú está abierto
body.udp-megamenu-open {
    overflow: hidden;
}

.udp-megamenu {
    position: fixed;
    inset: 0;
    z-index: 1000;
    background-color: $white;  // light theme
    color: $dark-1;
    overflow-y: auto;
    display: flex;
    flex-direction: column;

    &[hidden] { display: none; }

    // ----------------------------------------------------------------------
    // TOP BAR INTERNO (× Cerrar + logo)
    // ----------------------------------------------------------------------
    &__top {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        align-items: center;
        gap: $space-md;
        padding: $space-md $space-3xl;
        border-bottom: 1px solid rgba($dark-1, 0.1);

        @include media-down(md) {
            padding: $space-sm $space-md;
        }
    }

    &__close {
        justify-self: start;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: transparent;
        border: 0;
        cursor: pointer;
        font-family: $font-family-body;
        font-size: 14px;
        font-weight: 500;
        color: $dark-1;

        &-circle {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            border-radius: 9999px;
            border: 1px solid $dark-1;
            transition: background-color $transition-base, color $transition-base;
        }

        &:hover &-circle,
        &:focus-visible &-circle {
            background-color: $dark-1;
            color: $white;
            outline: none;
        }
    }

    &__logo {
        justify-self: center;
        display: inline-flex;
        align-items: center;

        img { height: 32px; width: auto; display: block; }
        &-text {
            font-family: $font-family-display;
            font-size: 16px;
            font-weight: 500;
        }
    }

    // ----------------------------------------------------------------------
    // BODY 3 COLUMNAS
    // ----------------------------------------------------------------------
    &__body {
        flex: 1;
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        gap: 0;
        padding: $space-2xl $space-3xl;
        max-width: 1440px;
        width: 100%;
        margin: 0 auto;
        position: relative;

        @include media-down(lg) {
            grid-template-columns: 1fr;
            padding: $space-md;
            gap: $space-xl;
        }
    }

    &__primary {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: $space-2xs;

        @include media-down(lg) {
            border-bottom: 1px solid rgba($dark-1, 0.1);
            padding-bottom: $space-md;
        }
    }

    &__primary-item {
        // Default: gris suave
        &-btn,
        &-btn[aria-expanded] {
            background: transparent;
            border: 0;
            padding: $space-2xs 0;
            text-align: left;
            font-family: $font-family-display;
            font-weight: 500;
            font-size: 28px;
            line-height: 1.15;
            color: rgba($dark-1, 0.4);
            cursor: pointer;
            transition: color $transition-base;
            width: 100%;

            @include media-down(md) { font-size: 22px; }

            &:hover, &:focus-visible {
                color: $dark-1;
                outline: none;
            }
        }

        &--active &-btn {
            color: $dark-1;
            font-weight: 600;
        }
    }

    // ----------------------------------------------------------------------
    // DETAIL PANELS (col 2 + 3)
    // ----------------------------------------------------------------------
    &__detail {
        display: none;

        &--active {
            display: contents;  // mantiene el grid del parent (col 2 = submenu, col 3 = externos)
        }

        &[hidden] { display: none !important; }

        @include media-down(lg) {
            grid-column: 1;
            display: none;
            flex-direction: column;
            gap: $space-md;
            padding-top: $space-md;
            border-top: 1px solid rgba($dark-1, 0.1);

            &--active { display: flex; }
        }
    }

    &__submenu,
    &__externos {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: $space-2xs;
    }

    &__submenu-link,
    &__externos-link {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: $space-2xs 0;
        font-family: $font-family-body;
        font-size: 16px;
        font-weight: 500;
        color: $dark-1;
        text-decoration: none;
        transition: color $transition-base;

        &:hover, &:focus-visible {
            color: $brand-blue;
            outline: none;
        }

        svg { flex-shrink: 0; }
    }

    &__submenu-item--main &__submenu-link {
        font-weight: 600;
    }

    &__externos-link {
        font-size: 14px;
        color: rgba($dark-1, 0.7);
    }

    // ----------------------------------------------------------------------
    // FOOTER
    // ----------------------------------------------------------------------
    &__footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: $space-md;
        padding: $space-md $space-3xl;
        border-top: 1px solid rgba($dark-1, 0.1);
        flex-wrap: wrap;

        @include media-down(md) {
            padding: $space-md;
            flex-direction: column;
            align-items: flex-start;
            gap: $space-sm;
        }
    }

    &__quick-links {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        gap: $space-lg;
        flex-wrap: wrap;

        @include media-down(md) {
            gap: $space-sm $space-md;
        }
    }

    &__quick-link {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        font-family: $font-family-body;
        font-size: 14px;
        font-weight: 500;
        color: $dark-1;
        text-decoration: none;
        transition: color $transition-base;

        &:hover, &:focus-visible {
            color: $brand-blue;
            outline: none;
        }
    }

    &__socials {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        gap: $space-2xs;
    }

    &__social-link {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 36px;
        height: 36px;
        border-radius: 9999px;
        border: 1px solid $dark-1;
        color: $dark-1;
        text-decoration: none;
        transition: background-color $transition-base, color $transition-base;

        &:hover, &:focus-visible {
            background-color: $dark-1;
            color: $white;
            outline: none;
        }
    }

    &__empty {
        padding: $space-2xl $space-3xl;
        font-family: $font-family-body;
        font-size: 14px;
        color: rgba($dark-1, 0.6);
    }
}
```

- [x] **Step 2: Importar en main.scss**

Añadir después del último `@import "layouts/...";`:

```scss
@import "layouts/mega-menu";
```

- [x] **Step 3: Build**

```bash
cd /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme && npm run build 2>&1 | tail -3
```

---

## Task 5: E2E + commit

- [x] **Step 1: Verificar markup**

```bash
TS=$(date +%s)
echo "=== Cualquier página renderiza el panel oculto por defecto ==="
curl -s "http://localhost:8888/udp/?nocache=$TS" | grep -oE 'udp-megamenu[a-z_-]*|data-udp-megamenu-(toggle|close|item|detail)' | sort -u | head -25
echo ""
echo "=== Panel hidden? (esperado hidden attr presente) ==="
curl -s "http://localhost:8888/udp/?nocache=$TS" | grep -oE 'id="udp-megamenu-panel"[^>]*' | head -1
echo ""
echo "=== Cantidad de items principales (depende de cuántos haya en ACF) ==="
curl -s "http://localhost:8888/udp/?nocache=$TS" | grep -cE 'data-udp-megamenu-item'
```

Expected: classes `udp-megamenu*` presentes, panel con `hidden` attribute por default.

- [x] **Step 2: Si no hay items en ACF, seed temporal para verificar**

```bash
export MYSQL_PWD=root
SOCK=/Applications/MAMP/tmp/mysql/mysql.sock
COUNT=$(/Applications/MAMP/Library/bin/mysql80/bin/mysql --socket=$SOCK -uroot udp -sN -e "
SELECT COUNT(*) FROM wp_fnku4yoptions WHERE option_name LIKE 'options_menu_principal%' AND option_value != '' AND option_value != '0';")
echo "Menu items en options: $COUNT"
```

Si COUNT > 0, los items ya existen y el panel mostrará el menú real. Si COUNT = 0, documentar como pendiente para que el cliente cargue items en `Options → Header & Mega-menú`.

- [x] **Step 3: Cleanup + MEMORY + commit**

```bash
rm -f /tmp/acf-sync-header-f8.php /tmp/field-quick-links.json
```

Append a MEMORY.md:

```markdown

### 2026-04-29 — F8 Mega-menú completo

**Hechos**:
- Partial nuevo `template-parts/header/mega-menu.php` insertado en `header.php` después del top-bar. Panel light fixed full-viewport con 3 columnas (items principales / submenu activo / links externos) + top-bar interno (Cerrar + logo) + footer (quick links + social).
- ACF group `group_options_header` extendido con field `mega_menu_quick_links` (repeater titulo + link + new_tab) para los links del footer del mega-menú (Bibliotecas, Estudiantes, Alumni, Servicios, UDP University, etc.).
- JS module nuevo `src/js/modules/mega-menu.js` con state machine: open/close (toggle button, ESC key, close button), active item via click/hover/focus, lock body scroll, focus management básico (return to last focused on close).
- Primer item activo por defecto cuando el panel se abre. Hover y focus sobre item de col 1 cambia el detail panel en cols 2 y 3 sin click necesario.
- SCSS layout `_mega-menu.scss` con grid 3-col desktop → 1-col mobile (cols 2+3 collapsibles bajo el item activo).

**Decisiones clave**:
- `display: contents` en `.udp-megamenu__detail--active` para que el detail panel "desaparezca" del flow del grid y sus children (submenu + externos) sean direct children del grid del parent — alinea col 2 y col 3 sin div intermediario.
- Hover/focus sobre items col 1 cambia detail (UX moderna). Click sigue funcionando para keyboard users.
- Body class `udp-megamenu-open` se añade al `<body>` y `<html>` para lock scroll + cualquier override de header z-index.
- Primer item activo por defecto en lugar de "ningún item activo" → mejor UX, el usuario ve algo de contenido al abrir.
- Quick links en footer del megamenú son separados del menu_principal — campo ACF nuevo `mega_menu_quick_links` evita reutilizar links_externos del menu_principal (cada item tiene los suyos).

**Pendientes**:
- El cliente debe cargar items en `Options → Header & Mega-menú → Menu Principal` y `Mega-menú: Quick Links`. Si no hay data, el panel muestra el empty state.
- F9: Home. F10: Polish. F11: Switch local.
```

Commit:

```bash
cd /Applications/MAMP/htdocs/udp/cms/wp-content/themes/starter-theme
git add \
  acf-json/group_options_header.json \
  template-parts/header/mega-menu.php \
  header.php \
  src/js/modules/mega-menu.js \
  src/js/main.js \
  src/scss/layouts/_mega-menu.scss \
  src/scss/main.scss \
  MEMORY.md
git commit -m "$(cat <<'EOF'
feat(header): F8 mega-menú completo

- Partial template-parts/header/mega-menu.php insertado en header.php.
  Panel light fixed full-viewport con 3 cols (items / submenu / externos)
  + top-bar interno (Cerrar + logo) + footer (quick links + social).
- ACF group_options_header extendido con field mega_menu_quick_links
  repeater para los links del footer del mega-menú.
- JS module mega-menu.js con state machine: open/close (toggle, ESC,
  close button), active item via click/hover/focus, lock body scroll.
- SCSS _mega-menu.scss con grid 3-col → 1-col mobile (display:contents
  en detail--active para alinear cols sin div intermedio).
- Primer item activo por default. Hover/focus en col 1 cambia detail.
EOF
)"
```

---

## Verification

1. Cualquier page → click "Menú" en top-bar → panel mega-menu se abre fullscreen con primer item activo.
2. Click en otro item de col 1 (o hover) → cambia detail panel en cols 2+3.
3. ESC o click en × Cerrar → panel se cierra, focus vuelve al toggle.
4. Body scroll bloqueado mientras el panel está abierto.
5. Mobile: cols 2+3 caen bajo el item activo verticalmente.
6. Si admin no ha cargado items en Options → empty state "No hay items configurados".
